// Import the central menu registry
import { menus } from '/js/pages/menu.js';
import { SYSTEM } from '../strings.js';

// Register this menu configuration with the central registry
menus['dashboard-menu'] = SYSTEM.MENU.DASHBOARD;
